import { User } from './User';
export declare class CommonUser extends User {
    constructor(id: number, name: string, email: string, password: string, description: string, address: any, createdAt?: Date);
}
